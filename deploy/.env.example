# codeprism.dev configuration
# Copy to .env and customize

# Your domain (required for HTTPS)
CODEPRISM_DOMAIN=codeprism.dev

# Enable multi-tenant mode for SaaS hosting
CODEPRISM_MULTI_TENANT=true

# Company name shown in dashboard
CODEPRISM_COMPANY_NAME=codeprism

# LLM API keys — fill in the provider(s) you use
# At least one is required for LLM-enriched knowledge cards
GOOGLE_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
DEEPSEEK_API_KEY=

# Admin secret for tenant management endpoints (list, delete, rotate-key)
# setup.sh generates this automatically; to create manually: openssl rand -hex 32
CODEPRISM_ADMIN_KEY=

# Enable anonymous telemetry collection from self-hosted instances
# CODEPRISM_TELEMETRY=true

# Cloudflare API token (only needed for wildcard subdomain SSL)
# CF_API_TOKEN=
