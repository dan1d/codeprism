services:
  - type: web
    name: srcmap
    runtime: docker
    plan: starter
    envVars:
      - key: CODEPRISM_PORT
        value: 4000
      - key: CODEPRISM_DB_PATH
        value: /data/codeprism.db
    disk:
      name: srcmap-data
      mountPath: /data
      sizeGB: 1
