services:
  codeprism:
    build: .
    ports:
      - "${CODEPRISM_PORT:-4000}:4000"
    volumes:
      - codeprism-data:/data
    environment:
      - CODEPRISM_PORT=4000
      - CODEPRISM_DB_PATH=/data/codeprism.db
    restart: unless-stopped

volumes:
  codeprism-data:
